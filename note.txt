This file acts as a median of communiction between developers : 
Following are the notes :
// Common color constants to use across all UI classes
public class UiLayer.Support.UIConstants {
    public static final Color PRIMARY_COLOR = new Color(51, 153, 255);    // Main blue
    public static final Color SECONDARY_COLOR = new Color(245, 245, 245); // Light gray
    public static final Color ACCENT_COLOR = new Color(255, 69, 0);       // Warning/Delete red
    public static final Color TEXT_COLOR = new Color(33, 33, 33);         // Dark gray for text
}

// Update the update operation in your DAO class
public boolean updateWord(int id, WordDTO word) {
    // First verify if ID exists
    if (!checkIfIdExists(id)) {
        throw new IllegalArgumentException("ID " + id + " not found in database");
    }
    // Proceed with update if ID exists
    // ...existing update logic...
}
